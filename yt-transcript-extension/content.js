// Content script — runs on YouTube watch pages.
// Currently used as a marker so the extension has permission to
// execute scripts in the page context. The heavy lifting happens
// via chrome.scripting.executeScript from popup.js.
